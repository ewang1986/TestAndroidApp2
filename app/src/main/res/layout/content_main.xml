<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.wyz.testandroidapp.MainActivity"
    tools:showIn="@layout/activity_main">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hello World!"
        android:visibility="gone"/>
    <Button
        android:id="@+id/btn_kjFrame"
        style="@style/MainButton"
        android:text="@string/KJFrameForAndroid"
        />
    <Button
        android:id="@+id/btn_okHttp"
        android:layout_below="@id/btn_kjFrame"
        style="@style/MainButton"
        android:text="@string/OkHttp"
        />
    <Button
        android:id="@+id/btn_fresco"
        android:layout_below="@id/btn_okHttp"
        style="@style/MainButton"
        android:text="@string/Fresco"
        />
    <Button
        android:id="@+id/btn_service"
        android:layout_below="@id/btn_fresco"
        style="@style/MainButton"
        android:text="@string/system_service"
        />
</RelativeLayout>
